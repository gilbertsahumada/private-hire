import type { ReactNode } from 'react';
export const metadata = { title: 'Confidential Agent Jobs — integration probe', description: 'Stage-one technical status. No escrow or payments.' };
export default function Layout({ children }: { children: ReactNode }) { return <html lang="en"><body style={{ margin: 0, background: '#10191c', color: '#e8eee9', fontFamily: 'system-ui, sans-serif' }}>{children}</body></html>; }
